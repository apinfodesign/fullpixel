//Single controller page
//Confused how to export the separate controllers 
//will want a separate controller per page

var picControllers = angular.module('picControllers', []);  //[] = what angular modules to request

/*picControllers.controller('NAME', ['$scope', constraint, 
	function($scope, constraint{

	})])
	*/